# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv

# Editor
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# Local config with secrets / state
config.toml
credentials.json
sync_state.json
sync.log
launchd.out.log
launchd.err.log

# Beads / Dolt local state (see .beads/.gitignore for in-tree rules)
.dolt/
*.db
.beads-credential-key
# Passive JSONL exports — issue state lives in the local Dolt DB
# (.beads/embeddeddolt/). Solo/local setup: don't track the churning exports.
.beads/issues.jsonl
.beads/interactions.jsonl
